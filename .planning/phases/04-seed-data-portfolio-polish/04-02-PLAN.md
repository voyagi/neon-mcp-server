---
phase: 04-seed-data-portfolio-polish
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [README.md, .env.example]
autonomous: true

must_haves:
  truths:
    - "Prospect scanning the README sees an architecture diagram and feature list within 10 seconds"
    - "Prospect can follow setup instructions to create Supabase project, run seed SQL, and connect to Claude Desktop in under 15 minutes"
    - "Demo conversation script shows problem-solving arc: schema discovery, summary, drill-down, action, verification"
    - "Claude Desktop config JSON is copy-paste ready with placeholder values for URL and key"
    - "README covers all 12 tools grouped by entity: Customers (4), Tickets (4), Products (2), Analytics (1), plus schema resource"
  artifacts:
    - path: "README.md"
      provides: "Portfolio-ready project documentation with setup guide, feature list, architecture diagram, and demo conversation"
      min_lines: 150
    - path: ".env.example"
      provides: "Environment variable template with placeholder values"
      contains: "SUPABASE_URL"
  key_links:
    - from: "README.md setup section"
      to: "seed/seed.sql"
      via: "setup instructions reference running seed.sql"
      pattern: "seed.sql"
    - from: "README.md config section"
      to: ".env.example"
      via: "setup instructions reference .env.example"
      pattern: ".env.example"
---

<objective>
Create a portfolio-grade README.md that showcases the TechStart CRM MCP server to Upwork prospects, including visual-first presentation, complete feature list, setup guide, Claude Desktop config, and a demo conversation script.

Purpose: The README is the primary sales tool for the Upwork portfolio. Prospects decide in 30 seconds whether this developer can build MCP integrations for them. Visual hook + feature list + working demo path = "hire this person."

Output: `README.md` and updated `.env.example`
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seed-data-portfolio-polish/04-CONTEXT.md
@.planning/phases/04-seed-data-portfolio-polish/04-RESEARCH.md
@CLAUDE.md (for database schema DDL and Claude Desktop config example)
@package.json
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create portfolio-grade README.md</name>
  <files>README.md</files>
  <action>
Create README.md with the following sections in this exact order. Use clean markdown with no badges. The README is a sales document, not a technical manual.

**Section 1: Title + Hook**
```markdown
# TechStart CRM — MCP Server

> Connect your business database to Claude using the Model Context Protocol.
> Natural language queries, CRUD operations, and analytics — all through conversation.
```

**Section 2: Architecture Diagram**
Mermaid flowchart showing: Claude Desktop/Code --stdio--> MCP Server (Node.js + TypeScript) --Supabase JS Client--> PostgreSQL (Supabase)

Use `graph LR` format. Include edge labels for transport/protocol. Style nodes with different colors for visual clarity. Keep it simple — 3 nodes, bidirectional flow.

**Section 3: Features**
Group by entity with tool names and one-line descriptions:

- **Customers** — list_customers (filter by status/company), get_customer (with ticket stats), create_customer, update_customer
- **Tickets** — list_tickets (filter by status/priority/customer), get_ticket (with customer details), create_ticket (link by ID or name), close_ticket (with resolution notes)
- **Products** — list_products (with price formatting), search_products (by name or category)
- **Analytics** — get_summary (dashboard stats: customer counts, ticket breakdown, product catalog, recent activity)
- **Schema** — `schema://tables` resource for database introspection

Format as a table or grouped lists — whichever looks cleaner.

**Section 4: Tech Stack**
Concise list: Node.js, TypeScript, @modelcontextprotocol/sdk, Supabase (PostgreSQL), Zod validation, stdio transport

**Section 5: Setup**
Step-by-step for a technical developer:

1. **Clone the repository**
   ```bash
   git clone https://github.com/voyagi/upwork-mcp-server.git
   cd upwork-mcp-server
   npm install
   ```

2. **Create a Supabase project**
   - Go to [supabase.com](https://supabase.com) and create a new project
   - In the SQL Editor, run the table creation SQL (include the full CREATE TABLE statements from CLAUDE.md — customers, products, tickets tables)
   - Then run the contents of `seed/seed.sql` to populate demo data

3. **Configure environment**
   ```bash
   cp .env.example .env
   ```
   Fill in SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from Supabase project settings > API

4. **Build and test**
   ```bash
   npm run build
   npm start
   ```

**Section 6: Claude Desktop Integration**
Copy-paste JSON config with the correct structure. Use generic placeholders:
```json
{
  "mcpServers": {
    "techstart-crm": {
      "command": "node",
      "args": ["/absolute/path/to/upwork-mcp-server/dist/index.js"],
      "env": {
        "SUPABASE_URL": "https://your-project-id.supabase.co",
        "SUPABASE_SERVICE_ROLE_KEY": "your-service-role-key-here"
      }
    }
  }
}
```
Include a note about where to find `claude_desktop_config.json`:
- macOS: `~/Library/Application Support/Claude/claude_desktop_config.json`
- Windows: `%APPDATA%\Claude\claude_desktop_config.json`

**Section 7: Demo Conversation**
Write a 5-7 exchange problem-solving arc. Format as bold **User:** and **Claude:** with italic *[Calls tool_name]* annotations.

Arc structure:
1. Schema discovery: "What data do you have access to?" — Claude calls schema://tables, describes the CRM tables
2. Overview: "How are things looking today?" — Claude calls get_summary, reports dashboard stats (customer counts, open ticket breakdown by priority, product catalog value)
3. Investigate: "Show me the urgent tickets" — Claude calls list_tickets with status/priority filter, presents the urgent items
4. Take action: "Close that SSL ticket — we renewed the certificate" — Claude calls close_ticket with resolution text, confirms closure
5. Create: "We just signed Pinnacle Retail as a new customer" — Claude calls create_customer, confirms record created
6. Verify: "What's the updated summary?" — Claude calls get_summary, shows updated counts reflecting the closure and new customer

Make Claude's responses feel natural — not just tool output dumps but interpreted summaries. Show Claude reasoning: "Based on the analytics, there are 3 urgent tickets that need attention..."

**Section 8: Database Schema**
Include condensed CREATE TABLE statements (from CLAUDE.md) so prospects can see the data model at a glance. Use a single SQL code block with all three tables.

**Section 9: Development**
```bash
npm run dev      # Run with hot reload (tsx watch)
npm run build    # Compile TypeScript
npm run start    # Run compiled version
npm run check    # Biome lint + format
```

**Section 10: License**
MIT License, one line.

**Style guidelines:**
- No emojis in headings or content
- No badges
- Clean, professional markdown
- Mermaid diagram wrapped in triple-backtick mermaid fence
- Code blocks with appropriate language tags (bash, json, sql)
- Use `---` horizontal rules to separate major sections if needed for visual breathing room
  </action>
  <verify>
1. README.md exists and has 150+ lines
2. Contains mermaid code fence for architecture diagram
3. Lists all 12 tools (list_customers, get_customer, create_customer, update_customer, list_tickets, get_ticket, create_ticket, close_ticket, list_products, search_products, get_summary) plus schema://tables resource
4. Setup section references seed/seed.sql and .env.example
5. Claude Desktop config JSON is valid and contains placeholder values
6. Demo conversation has 5+ exchanges with *[Calls tool_name]* annotations
7. Database schema section includes CREATE TABLE statements for all 3 tables
8. No emojis or badges present
  </verify>
  <done>
- README.md is a complete portfolio-ready document with hook, architecture diagram, features, setup, config, demo, and schema
- All 12 tools and schema resource are listed
- Setup guide is self-contained (clone → Supabase → seed → config → build)
- Demo conversation demonstrates problem-solving arc across 5-7 exchanges
- Claude Desktop config is copy-paste ready
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify .env.example completeness</name>
  <files>.env.example</files>
  <action>
Read the current .env.example. It should contain:

```
# Supabase
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
```

If the current file matches this pattern already, no changes needed. If it's missing the placeholder values or has incorrect format, update it to match.

The .env.example must:
- Have a comment header identifying the section
- Use descriptive placeholder values (not just empty strings)
- Match the variable names referenced in src/lib/supabase.ts
  </action>
  <verify>
1. .env.example contains SUPABASE_URL with a placeholder URL
2. .env.example contains SUPABASE_SERVICE_ROLE_KEY with a placeholder value
3. Variable names match what src/lib/supabase.ts reads from process.env
  </verify>
  <done>
- .env.example has both required environment variables with descriptive placeholder values
- Variable names are consistent with the codebase
  </done>
</task>

</tasks>

<verification>
1. README.md renders correctly in GitHub markdown preview (mermaid diagram, code blocks, bold/italic formatting)
2. All 12 MCP tools are listed with descriptions
3. Setup instructions are complete: clone, Supabase setup, seed data, env config, build, Claude Desktop config
4. Demo conversation script covers tool variety: read, write, analytics, schema introspection
5. Claude Desktop config JSON is syntactically valid
6. .env.example matches codebase expectations
7. No secrets, API keys, or real credentials in any file
</verification>

<success_criteria>
- Prospect reading the README in 30 seconds sees: what it does (hook), how it works (diagram), what it offers (features)
- Prospect following setup guide can get from zero to working MCP server connected to Claude Desktop in under 15 minutes
- Demo conversation shows Claude naturally reasoning through a support queue workflow, not just executing tool calls
- Everything is copy-paste ready — no interpretation needed
</success_criteria>

<output>
After completion, create `.planning/phases/04-seed-data-portfolio-polish/04-02-SUMMARY.md`
</output>
