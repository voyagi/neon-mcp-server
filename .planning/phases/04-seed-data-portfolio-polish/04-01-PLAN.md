---
phase: 04-seed-data-portfolio-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [seed/seed.sql]
autonomous: true

must_haves:
  truths:
    - "Seed file inserts 20+ customers from diverse fictional companies with mixed statuses (active/inactive/lead)"
    - "Seed file inserts 30+ tickets with varied subjects, priorities, statuses, and timestamp spread across 3-6 months"
    - "Seed file inserts 10+ products with SaaS-style pricing tiers and add-ons"
    - "Some customers have 2-3 related tickets showing narrative progression (issue → follow-up → escalation)"
    - "Closed tickets have closed_at timestamps and resolution text"
    - "Seed file runs without errors in Supabase SQL editor (no FK violations, no duplicate emails)"
  artifacts:
    - path: "seed/seed.sql"
      provides: "Complete demo dataset for TechStart CRM"
      contains: "INSERT INTO customers"
  key_links:
    - from: "seed/seed.sql ticket inserts"
      to: "seed/seed.sql customer inserts"
      via: "subquery FK resolution: (SELECT id FROM customers WHERE email = '...')"
      pattern: "SELECT id FROM customers WHERE email"
---

<objective>
Expand seed.sql from 10 customers / 10 tickets / 10 products to 20+ customers / 30+ tickets / 10+ products with realistic fictional data, narrative threads, and timestamp variety.

Purpose: Realistic demo data makes the analytics summary impressive and gives Claude believable content to work with during portfolio demonstrations. Generic "Customer 1" placeholder data signals a toy project; narrative threads signal production thinking.

Output: Fully rewritten `seed/seed.sql` ready for Supabase SQL editor execution.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-seed-data-portfolio-polish/04-CONTEXT.md
@.planning/phases/04-seed-data-portfolio-polish/04-RESEARCH.md
@seed/seed.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite seed.sql with expanded realistic data</name>
  <files>seed/seed.sql</files>
  <action>
Completely rewrite seed/seed.sql with the following structure. The file is data-only INSERTs (no CREATE TABLE). Keep the ALTER TABLE for the resolution column at the top.

**Header comment:**
```sql
-- TechStart CRM Demo Data
-- Run this in Supabase SQL editor after creating the tables
-- See README.md for full setup instructions
```

**ALTER TABLE (keep existing):**
```sql
ALTER TABLE tickets ADD COLUMN IF NOT EXISTS resolution text;
```

**Customers (20+ rows):**
- Believable fictional companies: e.g., "Meridian Logistics", "Cascade Health", "NovaBridge Software", "Apex Manufacturing", "Lumina Design Co", "Ironclad Security", etc.
- Realistic contact names and email patterns: firstname@company-domain.com
- Status distribution: ~14 active, ~3 inactive, ~3 lead — makes analytics interesting
- Use `created_at` with `now() - interval 'X days'` for timestamp spread over ~180 days
- Companies span multiple industries: logistics, health, software, manufacturing, design, security, finance, education, retail, consulting

**Products (10-12 rows):**
- SaaS-style pricing tiers: Starter Plan, Professional Plan, Enterprise Plan
- Add-ons: Priority Support, API Access, Custom Integrations, Data Analytics, White-Label
- Monthly subscriptions priced in cents: Starter ($49/mo = 4900), Pro ($149/mo = 14900), Enterprise ($499/mo = 49900)
- Add-ons: $29-$199/mo range
- Each product has a clear description explaining what's included

**Tickets (30+ rows):**
- Use subquery FK resolution: `(SELECT id FROM customers WHERE email = '...')`
- **Narrative threads** — at least 4 customers should have 2-3 related tickets showing progression:
  - Customer A: "Login issues" (closed, 45 days ago) → "Account migration request" (in_progress, 5 days ago)
  - Customer B: "API rate limiting" (closed, 30 days ago) → "API quota upgrade request" (open, 2 days ago)
  - Customer C: "Data export broken" (open, 10 days ago) → "Follow-up: data export still failing" (open, 3 days ago) → escalation pattern
  - Customer D: "Billing discrepancy" (closed, 60 days ago) → "New billing question" (open, 1 day ago)
- **Status distribution:** ~10 open, ~8 in_progress, ~12 closed — makes analytics realistic
- **Priority distribution:** ~5 urgent, ~10 high, ~10 medium, ~5 low
- Use `created_at` with `now() - interval 'X days'` for each ticket
- Closed tickets: UPDATE at bottom to set `closed_at` and `resolution` text
- Include realistic IT/SaaS subjects: SSL cert issues, API errors, billing questions, onboarding help, performance degradation, feature requests, integration bugs

**Closing statements:**
```sql
-- Close resolved tickets with timestamps and resolutions
UPDATE tickets SET
  closed_at = created_at + interval '2 days',
  resolution = 'Resolved: ...'
WHERE status = 'closed' AND subject = '...';
```
Use individual UPDATE statements per closed ticket so each gets a specific resolution and realistic closed_at (created_at + 1-5 days offset).

**Important constraints:**
- Every email in customer inserts must be unique
- Every ticket's customer_id subquery must reference an email that exists in the customers INSERT
- No CREATE TABLE or DROP TABLE statements
- All company names must be fictional (not real companies)
  </action>
  <verify>
1. Count customer rows: grep -c "^  ('" seed/seed.sql should show 20+
2. Count product rows: verify 10+ product inserts
3. Count ticket inserts: verify 30+ ticket rows
4. Verify no CREATE TABLE statements: grep "CREATE TABLE" seed/seed.sql should return nothing
5. Verify FK resolution pattern: all ticket inserts use (SELECT id FROM customers WHERE email = ...)
6. Verify closed tickets have UPDATE statements with resolution text
7. Verify timestamp spread: grep for "interval" shows variety of day offsets
8. Verify status variety: grep for 'active', 'inactive', 'lead' in customers; 'open', 'in_progress', 'closed' in tickets
  </verify>
  <done>
- seed/seed.sql contains 20+ customer inserts with diverse companies and mixed statuses
- seed/seed.sql contains 10+ product inserts with SaaS pricing tiers and add-ons
- seed/seed.sql contains 30+ ticket inserts with narrative threads and priority variety
- Closed tickets have individual UPDATE statements with specific resolution text and closed_at timestamps
- All foreign keys use subquery resolution (no hardcoded UUIDs)
- Timestamps spread across 3-6 months using interval arithmetic
  </done>
</task>

</tasks>

<verification>
1. Open seed/seed.sql and verify it starts with ALTER TABLE (no CREATE TABLE)
2. Count: 20+ unique customer emails, 30+ ticket inserts, 10+ products
3. Verify at least 4 customers have multiple tickets (narrative threads)
4. Verify status distribution: customers have active/inactive/lead mix; tickets have open/in_progress/closed mix
5. Verify all ticket subqueries reference emails that exist in the customers INSERT block
6. Verify closed tickets each have unique resolution text
</verification>

<success_criteria>
- seed.sql runs cleanly in Supabase SQL editor with no FK violations
- Analytics get_summary would return varied, impressive numbers (not all same status/priority)
- Data looks professional — prospects browsing the repo see realistic company names and ticket subjects, not "test data"
</success_criteria>

<output>
After completion, create `.planning/phases/04-seed-data-portfolio-polish/04-01-SUMMARY.md`
</output>
