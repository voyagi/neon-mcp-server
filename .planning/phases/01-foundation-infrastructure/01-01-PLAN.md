---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase.ts
  - src/index.ts
autonomous: true

must_haves:
  truths:
    - "Server refuses to start when SUPABASE_URL or SUPABASE_SERVICE_ROLE_KEY is missing, printing setup instructions to stderr"
    - "Server tests database connectivity at startup and exits with actionable error if unreachable"
    - "Error messages never expose the actual Supabase URL — only reference the environment variable name"
    - "All logging uses console.error (stderr) — zero console.log/console.info/console.warn calls in entire codebase"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Hardened Supabase client with env validation and connection test"
      exports: ["supabase", "validateConnection"]
    - path: "src/index.ts"
      provides: "Startup sequence calling validateConnection before transport connect"
      contains: "validateConnection"
  key_links:
    - from: "src/index.ts"
      to: "src/lib/supabase.ts"
      via: "import { validateConnection }"
      pattern: "validateConnection\\(\\)"
    - from: "src/lib/supabase.ts"
      to: "supabase.from"
      via: "test query at startup"
      pattern: "from\\(['\"]customers['\"]\\)"
---

<objective>
Harden the Supabase client with environment variable validation and startup connection testing, and audit all logging to ensure stderr-only output.

Purpose: Foundation for every database operation in Phases 2-4. Without validated connectivity at startup, tools will fail with cryptic errors on first use. Without stderr-only logging, the MCP protocol stream gets corrupted and the server becomes unresponsive in Claude Desktop.

Output: A `src/lib/supabase.ts` that validates env vars and exports a `validateConnection()` function, and an updated `src/index.ts` that calls it before connecting the transport.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
@src/lib/supabase.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Supabase client with env validation and connection testing</name>
  <files>src/lib/supabase.ts</files>
  <action>
Rewrite `src/lib/supabase.ts` to:

1. **Validate environment variables at module load:**
   - Check that `SUPABASE_URL` exists and is non-empty. If missing, call `console.error()` with a message like: "Missing required environment variable: SUPABASE_URL. See seed/setup.md for configuration instructions." then `process.exit(1)`.
   - Check that `SUPABASE_SERVICE_ROLE_KEY` exists and is non-empty. Same pattern.
   - Do NOT log the actual values of either variable (security).

2. **Create Supabase client** only after validation passes. Export as named export `supabase`.

3. **Export an async `validateConnection()` function** that:
   - Runs a test query: `supabase.from('customers').select('id').limit(1)`
   - On success: `console.error("Supabase connection verified")` — confirms connectivity.
   - On error: Catch the error, `console.error()` a sanitized message: "Failed to connect to Supabase database. Verify SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are correct. If tables are missing, run the seed script — see seed/setup.md." Do NOT include the raw Supabase error message (may contain the URL). Then `process.exit(1)`.

4. **Use `console.error()` exclusively.** No `console.log`, `console.info`, or `console.warn`.

Keep the module simple — no classes, no configuration objects. Just validate, create client, export.
  </action>
  <verify>
Run `npx biome check src/lib/supabase.ts` — no lint or format errors.
Grep the file for `console.log`, `console.info`, `console.warn` — zero matches.
Confirm `validateConnection` and `supabase` are exported (grep for `export`).
  </verify>
  <done>
`src/lib/supabase.ts` exports `supabase` client and `validateConnection()` function. Env vars are validated at module load. Connection test queries `customers` table. All error messages reference setup docs and hide actual Supabase URL. Only `console.error` is used.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update startup sequence and audit logging across codebase</name>
  <files>src/index.ts</files>
  <action>
1. **Update `src/index.ts` startup sequence:**
   - Import `validateConnection` from `./lib/supabase.js`
   - In the `main()` function, call `await validateConnection()` BEFORE creating the transport and connecting. This ensures the database is reachable before the MCP server starts accepting tool calls.
   - Keep the existing `console.error("TechStart CRM MCP server running on stdio")` message AFTER successful connection and transport setup.
   - Keep the existing `.catch()` handler for unexpected startup errors.

   The startup order should be:
   ```
   1. createServer()
   2. await validateConnection()   <-- NEW
   3. new StdioServerTransport()
   4. await server.connect(transport)
   5. console.error("TechStart CRM MCP server running on stdio")
   ```

2. **Audit entire `src/` directory for stdout pollution:**
   - Grep all `.ts` files in `src/` for `console.log`, `console.info`, `console.warn`, `process.stdout.write`.
   - If any found, replace with `console.error` or remove.
   - Currently the codebase should be clean (existing code already uses `console.error`), but verify.

No other files should need changes — `server.ts` and `lib/types.ts` don't have logging.
  </action>
  <verify>
Run `npm run build` — TypeScript compiles without errors.
Run `npx biome check src/` — no lint or format errors.
Grep entire `src/` for `console.log|console.info|console.warn|process.stdout.write` — zero matches.
Verify `validateConnection` is called in `src/index.ts` before `server.connect`.
  </verify>
  <done>
`src/index.ts` calls `validateConnection()` before connecting the transport. The entire `src/` directory has zero stdout writes — all logging uses `console.error` exclusively. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` compiles without errors
2. `npx biome check src/` passes with no issues
3. `grep -r "console.log\|console.info\|console.warn\|process.stdout" src/` returns empty
4. `grep "validateConnection" src/index.ts` shows the function is called
5. `grep "validateConnection" src/lib/supabase.ts` shows the function is exported
6. `grep "SUPABASE_URL" src/lib/supabase.ts` shows env var validation (but NOT the actual URL value)
</verification>

<success_criteria>
- Server startup validates environment variables and database connection before accepting MCP protocol messages
- All error messages are helpful, actionable, and never expose sensitive values
- Zero stdout pollution across the entire codebase
- TypeScript compiles cleanly, Biome passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
