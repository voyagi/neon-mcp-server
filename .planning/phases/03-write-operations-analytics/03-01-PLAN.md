---
phase: 03-write-operations-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/tools/customers.ts]
autonomous: true

must_haves:
  truths:
    - "create_customer inserts a new customer and returns the full record with generated id and created_at"
    - "create_customer with a duplicate email returns a clear error mentioning the email, not a raw Postgres message"
    - "update_customer modifies only the provided fields and returns the complete updated record"
    - "update_customer with a non-existent UUID returns a not-found error"
    - "update_customer changing email to an existing one returns a clear duplicate error"
  artifacts:
    - path: "src/tools/customers.ts"
      provides: "create_customer and update_customer tools"
      contains: "create_customer"
    - path: "src/tools/customers.ts"
      provides: "update_customer tool"
      contains: "update_customer"
  key_links:
    - from: "src/tools/customers.ts"
      to: "supabase.from('customers').insert()"
      via: ".insert().select() chain"
      pattern: "\\.insert\\(.*\\)\\.select\\("
    - from: "src/tools/customers.ts"
      to: "supabase.from('customers').update()"
      via: ".update().eq().select() chain"
      pattern: "\\.update\\(.*\\)\\.eq\\(.*\\)\\.select\\("
    - from: "src/tools/customers.ts"
      to: "error.code === '23505'"
      via: "unique constraint detection"
      pattern: "error\\.code.*23505"
---

<objective>
Add create_customer and update_customer write tools to the existing customer tools module.

Purpose: Enable Claude to create new customers and update existing customer records through natural conversation, completing the customer CRUD operations.
Output: Two new MCP tools registered in customers.ts alongside the existing read tools.
</objective>

<execution_context>
@C:\Users\Eagi\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Eagi\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-write-operations-analytics/03-CONTEXT.md
@.planning/phases/03-write-operations-analytics/03-RESEARCH.md

@src/tools/customers.ts
@src/lib/validation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement create_customer tool</name>
  <files>src/tools/customers.ts</files>
  <action>
Add a `create_customer` tool inside the existing `registerCustomerTools` function, after the `get_customer` tool.

Tool registration:
- Name: `create_customer`
- Description: "Create a new customer with name, email, optional company and status"
- Params: Use Zod inline in server.tool() matching CreateCustomerSchema shape:
  - `name`: z.string().min(1, "Customer name is required")
  - `email`: z.string().email("Invalid email format")
  - `company`: z.string().optional()
  - `status`: CustomerStatus.optional()

Implementation:
1. Destructure args: { name, email, company, status }
2. Insert with `.insert({ name, email, company: company || null, status: status || 'active' }).select()`
3. Error handling:
   - If `error.code === '23505'`: return `A customer with email "${email}" already exists`
   - Other errors: return `Database error: ${error.message}`
4. Success: return `JSON.stringify(data[0], null, 2)` — the full created record

All responses use the standard MCP format: `{ content: [{ type: "text", text: ... }] }`
  </action>
  <verify>Run `npm run check` — no Biome errors. Read src/tools/customers.ts and confirm create_customer tool is registered with .insert().select() chain and error.code === '23505' check.</verify>
  <done>create_customer tool exists in customers.ts, inserts via .insert().select(), handles duplicate email with clear message, returns full created record on success.</done>
</task>

<task type="auto">
  <name>Task 2: Implement update_customer tool</name>
  <files>src/tools/customers.ts</files>
  <action>
Add an `update_customer` tool inside `registerCustomerTools`, after `create_customer`.

Tool registration:
- Name: `update_customer`
- Description: "Update customer fields by ID. Only send the fields you want to change."
- Params: Use Zod inline matching UpdateCustomerSchema shape:
  - `id`: z.string().uuid("Customer ID must be a valid UUID")
  - `name`: z.string().optional()
  - `email`: z.string().email("Invalid email format").optional()
  - `company`: z.string().optional()
  - `status`: CustomerStatus.optional()

Implementation:
1. Destructure args: { id, name, email, company, status }
2. Build partial update object — only include fields that are not undefined:
   ```
   const updates: Record<string, unknown> = {};
   if (name !== undefined) updates.name = name;
   if (email !== undefined) updates.email = email;
   if (company !== undefined) updates.company = company;
   if (status !== undefined) updates.status = status;
   ```
3. If `Object.keys(updates).length === 0`, return error: "No fields provided to update"
4. Execute `.update(updates).eq('id', id).select()`
5. Error handling:
   - If `error.code === '23505'`: return `Email "${email}" is already taken by another customer`
   - Other errors: return `Database error: ${error.message}`
6. If `!data || data.length === 0`: return `Customer not found: ${id}`
7. Success: return `JSON.stringify(data[0], null, 2)`
  </action>
  <verify>Run `npm run check` — no Biome errors. Run `npm run build` — TypeScript compiles cleanly. Read src/tools/customers.ts and confirm update_customer builds partial update object and handles both not-found and duplicate-email cases.</verify>
  <done>update_customer tool exists in customers.ts, supports partial updates, handles not-found and duplicate email, returns full updated record on success. Both customer write tools compile without errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` compiles without errors
2. `npm run check` passes Biome lint/format
3. customers.ts exports registerCustomerTools containing 4 tools: list_customers, get_customer, create_customer, update_customer
4. create_customer uses .insert().select() and checks error.code === '23505'
5. update_customer builds partial update object and uses .update().eq().select()
</verification>

<success_criteria>
- create_customer and update_customer tools are registered in the existing registerCustomerTools function
- Both tools return full records via .select() chaining
- Duplicate email errors produce clear, user-friendly messages
- update_customer supports partial updates (only changed fields)
- Code passes npm run build and npm run check
</success_criteria>

<output>
After completion, create `.planning/phases/03-write-operations-analytics/03-01-SUMMARY.md`
</output>
